name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref to tag (commit SHA, branch name, or leave empty for HEAD of main)'
        required: false
        default: ''
      tag:
        description: 'Tag name (e.g., v2026.06.18)'
        required: true

permissions:
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Create and push tag (workflow_dispatch only)
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Check if tag already exists locally or remotely
          if git rev-parse ${{ github.event.inputs.tag }} >/dev/null 2>&1; then
            echo "Tag ${{ github.event.inputs.tag }} already exists locally, skipping creation"
          elif git ls-remote --tags origin | grep -q "refs/tags/${{ github.event.inputs.tag }}$"; then
            echo "Tag ${{ github.event.inputs.tag }} already exists remotely, fetching it"
            git fetch origin tag ${{ github.event.inputs.tag }}
          else
            echo "Creating and pushing new tag ${{ github.event.inputs.tag }}"
            git tag ${{ github.event.inputs.tag }}
            git push origin ${{ github.event.inputs.tag }}
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tools
        run: pip install build

      - name: Build package
        run: python -m build

      - name: Check if version exists on PyPI
        id: pypi_check
        run: |
          VERSION=$(git describe --tags --exact-match 2>/dev/null || echo "unknown")
          VERSION=${VERSION#v}  # Remove 'v' prefix
          if curl -s "https://pypi.org/pypi/sdlc-mcp/$VERSION/json" | grep -q "\"version\""; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Version $VERSION already exists on PyPI, skipping publish"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION does not exist on PyPI, will publish"
          fi

      - name: Publish to PyPI
        if: steps.pypi_check.outputs.exists == 'false'
        uses: pypa/gh-action-pypi-publish@release/v1

      - name: Check if GitHub Release exists
        id: release_check
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Release for $TAG already exists, skipping creation"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Release for $TAG does not exist, will create"
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Create GitHub Release
        if: steps.release_check.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          generate_release_notes: true
